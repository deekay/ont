{
  "name": "@ont/publisher",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "npm run build -w @ont/protocol && npm run build -w @ont/core && tsc -p tsconfig.json --noEmit && vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ont/core": "file:../../packages/core",
    "@ont/protocol": "file:../../packages/protocol",
    "bitcoinjs-lib": "^7.0.1",
    "ecpair": "^3.0.1",
    "tiny-secp256k1": "^2.2.4"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
