{
  "name": "@ont/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ONT B5 surface — the `ont` CLI (clean-build). A thin operator orchestrator: consumes L1-L4 APIs and reimplements no rules; holds no keys and never signs (signing → B5-WALLET). First slice = read/query commands over an injected CliReadPort (mocked in tests; no live network). See docs/core/B5_CLI_CLASSIFICATION.md.",
  "bin": {
    "ont": "./dist/apps/cli/src/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build -w @ont/protocol && npm run build -w @ont/bitcoin && npm run build -w @ont/wire && npm run build -w @ont/consensus && npm run build -w @ont/launch-config && npm run build -w @ont/adapter-header && npm run build -w @ont/light-client && npm run build -w @ont/name-state-store && npm run build -w @ont/adapter-resolver && vitest run"
  },
  "dependencies": {
    "@ont/wire": "file:../../packages/wire",
    "@ont/protocol": "file:../../packages/protocol",
    "@ont/consensus": "file:../../packages/consensus",
    "@ont/light-client": "file:../../packages/light-client",
    "@ont/launch-config": "file:../../packages/launch-config",
    "@ont/adapter-resolver": "file:../../packages/adapter-resolver"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
