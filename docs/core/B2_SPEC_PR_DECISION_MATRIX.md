# B2 Spec-PR Decision Matrix — eyes-open batch-approval for DK

> **Normativity: `analysis`** — the decision-ready matrix for the **~30 remaining
> spec-PRs** (PR-5..15, PR-17..36) so DK approves them eyes-open rather than as an
> opaque batch (ChatLunatique guardrail #7). The 5 P0 forks (PR-1/2/3/4/16) + PR-23
> are already RULED — see [DECISIONS.md](./DECISIONS.md) #51–#56. Drafted by
> ChatLunatique, refined by ClaudeleLunatique (PR-21, PR-34). Each row: exact rule
> text · creates-new-consensus-law? · alternative rejected · vectors unlocked /
> review focus. **DK approves "as recommended" or rules individual rows; nothing
> here is ratified until DK approves.** The high-risk boundary/reorg/finality rows
> (PR-9, PR-10, PR-13, PR-23, PR-35) are called out below the table.

## Matrix

| PR | Exact proposed rule text | New consensus law? | Alternative rejected | Vectors unlocked / review focus |
| --- | --- | --- | --- | --- |
| PR-5 | First-anchor-wins means earliest **valid** anchor: accepted, DA-eligible, and gate-fee-covered. A forfeited, DA-excluded, or fee-failing anchor confers no priority; a later eligible re-anchor starts a fresh window. Same `(name, owner)` re-claims are idempotent. | Yes | Earliest merely anchored wins even if later ineligible. | A12, D5, B8, B11 priority halves. Pairs with PR-1/#49 for enforceability; the rule definition is independent. |
| PR-6 | A competing cheap claim is a DA-valid claim for the same canonical name with a **distinct owner key**. A same `(name, owner)` duplicate is idempotent, not a collision/nullifier. | Yes | Counting byte-duplicate or re-anchor attempts by the same owner as competitors. | B5; pairs with PR-5 and nullification rules. |
| PR-7 | Nullification counts only DA-valid claims and runs only after the relevant DA verdicts are resolvable. Anchored-withheld or DA-excluded claims do not nullify an honest claim. | Yes | Raw anchored count before DA resolution. | D10, F11 count basis, B3 exact-one count basis. Depends on PR-1 and #49 for evidence/timing. |
| PR-8 | Nullification is per claim generation: two or more DA-valid cheap claims with no qualifying bond produce no owner and reopen the name for a fresh claim generation. The nullified name does not occupy the namespace. No cooldown is part of B2 unless a later named launch/anti-grief decision adds one. | Yes | Per-name permanent nullification; cooldown by default; barring the same parties from re-claiming. | B24, A11, B4 occupancy basis. Review focus: denial attrition is accepted residual from bond-opens (#37), not solved by hidden cooldown. |
| PR-9 | Kernel state is a pure function of the current best chain. On reorg, anchor height `h`, deadlines, eligibility, provisional state, release/reopen facts, ownership intervals, and affected refs re-derive from the new chain. Served evidence may be reused only if it remains verifier-checkable against the current-chain anchor; no first-seen or old-chain priority survives. | Yes | First-seen priority; old `h` sticks through reorg; off-chain receipt time decides; evidence from an orphaned anchor carries automatically. | A2, A3 reorg flag, Z4, Z5, S9 reorg half, G8. **High-risk:** companion to #49 but **not swallowed by #49**. |
| PR-10 | Settlement/ownership materialization has its **own** finality depth parameter, e.g. `SETTLEMENT_FINALITY_DEPTH`; it must **not** inherit DA `K` by analogy. Before that depth, settled ownership is reorg-mutable; at/after it, the materialized owner/live-bond verdict is stable under the B2 replay model. | Yes | Reuse #49 `K`; zero-depth finality; adapter/resolver decides stability. | B13, B17 depth half, S9 stability half. **High-risk:** must stay separate from #49. |
| PR-11 | `settlementHeight` is the first height after the auction close at which the winner can materialize, subject to the settlement-finality rule. `maturityHeight = settlementHeight + MATURITY_BLOCKS`. `requiredBondSats` for the name is the winning bid amount, not the global floor. | Yes | Maturity anchored to winning-bid height; global floor as required bond after settlement; legacy close+1 left implicit. | S2, S3, S10, X/S/R maturity consumers. Needs exact close relation from PR-19/29. |
| PR-12 | A name is mature at any evaluated height `H >= maturityHeight`; the exact maturity height is in-window/mature. | Yes | Strictly `H > maturityHeight`. | S14, X8. Depends on PR-11 and PR-13 boundary convention. |
| PR-13 | One kernel boundary convention for non-DA windows: a stated deadline "by height `h+X`" includes events at `h+X`; transitions triggered by that boundary evaluate after all block-`h+X` events have applied, so `h+X+1` is the first out-of-window height. Apply to notice close, collision/bond landing, auction close, soft-close, maturity, and recovery-finalization boundaries unless a named PR states a narrower override. | Yes | Per-area mixed inclusive/exclusive conventions; importing #49 into non-DA windows without naming it. | A13, B2, B6, B20, F12, T6. **High-risk:** overlaps PR-12/19/29/35; do not collapse into #49. |
| PR-14 | Reconcile AUCTION floors to Decision #11: length-scaled mandatory bond-first treatment applies only to structurally scarce `<=4` char names; length `>=5` use the flat long-name floor/contention rules, concrete values launch-freeze. | Mostly doc reconciliation of ratified #11; parameter surface still consensus. | Old AUCTION halving table applied to longer names. | F6, F10, T8, B12/B21/F15 basis. Ensure no vector hardcodes placeholder floor values. |
| PR-15 | A `<=4` char name cannot be acquired by the cheap batched path: a short-name leaf is invalid, opens no notice window, creates no provisional owner. Under commitment-match (#52) leaf-drop, only that leaf is dropped; it still counts toward the committed-set fee sum if committed. | Yes | Allow cheap provisional short-name window; poison the whole batch by default; ignore the leaf for fee sum. | F15, B21. Depends on PR-14 and commitment-match (#52). |
| PR-17 | Define per-name state head as the txid/derived id of the most recent applied state-changing event. Transfer/RecoverOwner must target `prevStateTxid == current head`. Batched-final names receive a per-name unique initial head derived from the finalizing anchor plus name/leaf identity, not the shared anchor txid alone. | Yes | Shared anchor txid as head for every batched name; transfer target selected by adapter context. | X4, X5, X12, R4. Pairs with same-block-order (#55) and PR-18 intervals. **+ recovery interval-opening (V2/V5/V13): does the recovery interval open at invocation vs challenge-window close — reclassified here 2026-06-14 (not ruled by #50-b1).** |
| PR-18 | `ownershipRef` is the interval-opening reference: last state txid for L1-owner intervals, finalizing anchor-derived id for batched intervals. Value-record and descriptor chains die on interval rotation. Digest-covered `ownershipRef` must match the current interval; rail type is not otherwise an input. | Yes | Rail-specific value-record rules; records survive transfer/recovery interval rotation. | V14, R4, G8 interval half. Depends on PR-17 and PR-9 reorg derivation. |
| PR-19 | Auction economics: required minimum is `max(leader + ABS_RAISE, ceil(leader * (1 + PCT_RAISE)))`; accepted iff `bidAmountSats >= requiredMin` and strictly exceeds the current leader. Soft-close triggers for bids inside the final `SOFT_CLOSE_BLOCKS` and uses the soft percentage. A bid at close height is in-window; close+1 rejected; non-accepted bids mutate nothing; same-leader self-raise must clear the same formula. | Yes, values parameterized. | Position/effect mismatch from #35/AUCTION drift; floor/percentage ambiguity; non-accepted bids affecting state. | T8, B14, Q5/Q6/Q7/Q8/Q11/Q12/G4 close half. Boundary overlaps PR-13/29. |
| PR-20 | Decision #25 tie-break triggers when two otherwise valid same-lot bids have the same effective bid amount after applying auction rules. same-block-order (#55) supplies the deterministic winner among tied same-block bids; non-tied same-block bids apply sequentially and update the increment basis. | Yes, extension of #25. | Txid tie-break; bond size/unlockBlock as hidden tie-breakers; pre-block set evaluation. | T10, Z3, G1 tie predicate. Depends on same-block-order (#55). |
| PR-21 | **(refined)** A valid bid references a same-transaction `bondVout` that is a **standard spendable payment output** (not OP_RETURN, not attacker-burnable) controlled by the **bidder's key** under the auction witness rules, with value **`>=`** the bid amount (over-bonding allowed). A bond outpoint backs **at most one live bid** (one-bond-one-bid exclusivity). Same-bidder replacement is consensus-valid **only** when the later bid **spends the prior bid's bond outpoint** and creates the new full bond; a violating later bid is **ignored** (not allowed to replace, and does not invalidate the standing bid). `bidderCommitment`-keyed replacement **without** an outpoint spend is a non-consensus wallet convention, not consensus-bearing — replacement is purely outpoint-chain-based. | Yes | "Should" replacement; one bond backing multiple bids; `bidderCommitment`-only replacement without an outpoint chain; attacker-burnable bond script. | F13, F14, T12, Q8. Script/witness wording is now precise enough for B2/B3 evidence (CL flagged this row for my precision). |
| PR-22 | The bond reservation registry includes live name bonds, pending recovery successor bonds, accepted bids in open auctions, and any pending acquisition bond that can become live. No two live/pending claims may reserve the same outpoint. If two same-tx events try, same-block-order (#55) resolves or rejects later contenders. | Yes | Registry covers only already-owned names; accepted auction bids outside reservation; per-area registries. | S13, X7, R11 registry half. Depends on same-block-order (#55) and PR-23 valid-successor definition. |
| PR-23 | **RULED → settlement-bond-continuity (#56).** Winning bond must remain unspent confirmation→settlement; if it breaks before settlement, no owner materializes and the name reopens; runner-up not promoted; loser-side pre-settlement spends drop the bid from future effects. Denial-loop residual named and accepted. | Yes (ruled) | Runner-up-wins; stale loser bids still affect increments; dust successor; adapter-defined release height. | B17, B18, S7, S8, S10, S11, R17. See [DECISIONS.md](./DECISIONS.md) #56 + [PR23_DENIAL_LOOP.md](../research/PR23_DENIAL_LOOP.md). |
| PR-24 | Auction close, nullification, settlement, and release/reopen verdicts remain reorg-mutable until the settlement/reorg stability depth from PR-10; after that depth they are stable. Close/winner re-derivation on reorg is current-chain only. | Yes | Immediate finality at close; inherit #49 `K` silently; adapter chooses winner after reorg. | Z9, Z10. Depends on PR-9 and PR-10. |
| PR-25 | Adaptive extension may consume only enumerated deterministic canonical-chain facts, never manipulable unsafe-shrink signals. If no safe input set is ratified, delete adaptive shrink and keep only a monotonic height-keyed schedule. | Yes if retained; else deletion of unsafe candidate. | Market/manipulable adaptive shrink; implementation-defined input set. | Z11. P2, launch/auction policy. |
| PR-26 | Closed `CONSENSUS_PARAMS` manifest: member name, type, units, validity constraints, freeze status, coverage requirement. Unknown parameter references fail conformance. Each exercised member needs >=1 vector whose outcome changes under a second valid parameterization. | Yes | Open/free-form params; baked constants; untested params indistinguishable from unused fields. | G10, G11, R14, G9 surface. K/W/C members ride #49; values may stay launch-freeze. |
| PR-27 | Transcript source tags are a closed enum, including `bitcoin_l1_bid_transactions` for v1. A wire-malformed payload in an enumerated transcript is dropped as a non-bid/non-event; it does not abort the whole enumeration unless the proof bundle itself becomes incomplete. | Yes | Free-form source tags; one malformed chain payload aborts the whole transcript and DoSes auction enumeration. | T3, T4. Review focus: malformed-drop must not hide omitted valid bids. |
| PR-28 | Bid plaintext fields must match commitment preimages: on-chain `name`/`unlockBlock` equal the lot commitment preimage; `settlementLockBlocks` equals the per-lot required value; canonical `auctionId` grammar is `opening-{name}` or `reopen-{name}-after-{release_height}`; unrecognized auction ids match no lot; state-commitment preimage mismatches and exact-height mismatches reject. | Yes | Wire text and commitment preimage diverge; free-text phantom lots; stale state commitment accepted. | T5, T13, T14, S5, Q13, Q14, Q15. Overlaps PR-11 and PR-23 release height. |
| PR-29 | Auction close boundary: bids at the close height are valid and may extend if in soft-close; bids at close+1 are rejected. Settlement materializes directly from the winning bid owner key after the close rule and PR-10 finality; no separate winner-acknowledgement step in v1. | Yes | Strictly-before close; close-height bids rejected; separate undefined winner ack. | T6, T16, B13 settled-definition half, S16. Overlaps PR-13, PR-19, PR-10. |
| PR-30 | Launch height and genesis empty root `R0` are normative launch-freeze constants; from-genesis replay starts there, and checkpoint/incremental state must match replay from that origin. | Yes, launch-freeze constant. | Implicit repo/test origin; adapter-supplied genesis. | Z2. P2 launch-freeze. |
| PR-31 | If L1-settled and batched records for the same name are ever simultaneously evidenced, L1-settled ownership takes precedence as a defensive invariant; the intended stronger property is that the dual-record state is unreachable under B3–B7. | Yes defensive invariant. | Batched record wins or adapter chooses; rely solely on "unreachable" without a fail-safe. | B23. P2; depends on batched transition rules PR-17/PR-18. |
| PR-32 | Canonical value-record hash is the WIRE §8.1 digest; signature bytes are deliberately outside the content hash. No value-record ownership interval exists for an open notice-window or pre-DA-finality claim; records attach only to materialized ownership intervals. | Yes for interval rule; V9 is wire clarification. | Hash includes signature; provisional claimant can publish accepted records before ownership finalizes. | V9, V2/V13 non-#50 halves. Coordinates with #49 finality and PR-18. |
| PR-33 | Recovery descriptors form a per-ownership-interval chain: sequence starts at 1, increments by exactly 1, `previousDescriptorHash` equals the current head digest, null only for first. The armed descriptor is the current interval's chain head; invoke must reference that head. | Yes | Any descriptor with matching hash accepted; stale/non-head descriptor invokable. | R3, R6. Depends on PR-17/PR-18; signer identity is recovery-auth (#50). |
| PR-34 | **(refined, post recovery-auth (#50) b1)** RecoverOwner transaction-shape: `output[successorBondVout]` must be a valid successor bond under settlement-bond rules **and** its `scriptPubKey` MUST equal the script of the descriptor's retained `recoveryAddress` (descriptor v2 keeps `recoveryAddress` per WIRE §8.2a precisely for this successor-output check; the v2 `recoveryPubkey` authorizes the invoke *signature*, a separate leg). `chainTip*` fields are **advisory only** (no kernel meaning). Recovery evidence has a named witnessing deadline `h_r + W_r` with the validity constraint **`W_r <= challengeWindowBlocks`** so a recovery cannot finalize before its evidence deadline. Single `pendingRecovery` per name (second invoke ignored). The RecoverOwner `flags` registry assigns the **CANCEL bit = `0x01`** (wire-normative). The cancel-binding predicate is exact field-equality incl. `prevStateTxid` = the open pendingRecovery request txid (not a state txid), strict-before-deadline. | Yes | Invoke-builder prose as acceptance rule; `chainTip` as consensus input; multiple pending recoveries unspecified; flags left unregistered; successor-output script unbound to `recoveryAddress`. | R8, R11, R12, R13, R15, R16, R19, G6 evidence half. Depends on recovery-auth (#50, RATIFIED b1), settlement-bond-continuity (#56), PR-35. **+ X13 transfer-vs-recovery precedence during pendingRecovery — reclassified here 2026-06-14 (not ruled by #50-b1).** (CL flagged this row for my precision after the b1 WIRE amendment landed — now concrete.) |
| PR-35 | Recovery finalization is evaluated after all events in the finalize-height block apply. A valid cancel or owner-authorized transfer at the exact finalize height is in-window and prevents finalization; absent such an event, the recovery finalizes after that block. Same-block authorizing key follows the pre-finalization owner until the transition fires. | Yes | Finalize before processing the boundary block; exact-height cancel rejected; same-block-order (#55) alone decides recovery finalization. | G4 recovery-finalize flags. **High-risk:** overlaps PR-13, same-block-order (#55), recovery-auth (#50). |
| PR-36 | Owner-key transfers are free at the protocol level once a name is owned: no extra transfer-friction tax for cheap/accumulator-issued names. Immature bonded names must preserve bond continuity; nonexistent/provisional/nullified/live-auction names are not transferable. | Yes / decision entry. | Extra transfer friction for accumulator names; rail-specific transfer rights. | X11 and contributes to X12. P2 but easy decision. |

## Rows needing individual review before batch approval

- **PR-9** — must be current-chain re-derivation, not old-chain/receipt priority.
- **PR-10** — settlement finality depth must be named separately from DA `K`.
- **PR-13** — non-DA boundary convention must not silently import #49 everywhere.
- **PR-23** — RULED no-owner (#56); the denial-loop residual remains (named, accepted).
- **PR-35** — exact finalize-height event ordering must be explicit, not hidden inside same-block-order (#55).
